Using the memory pointer in c++
now can access the pointer with Python by ctypes


















































[solution]
        free(temp.vertices.data);
        free(temp.text_coor.data);
        free(temp.normals.data);
        free(temp.ind_v.data);
        free(temp.ind_t.data);
        free(temp.ind_n.data);
        std::printf("\nfinishing\n");
